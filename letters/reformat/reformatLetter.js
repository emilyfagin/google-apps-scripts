function reformatLetter() {
  let letterRange = SpreadsheetApp.getActive().getRange('full_letters!D2:D18930'); // 18930

  let updatedValues = letterRange.getValues();
  let total = 0;
  
  // Loop through each row in the src sheet
  for (let i = 0; i < updatedValues.length; i++) {
    
  }
}